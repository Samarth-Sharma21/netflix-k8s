apiVersion: v1
kind: Service
metadata:
  name: netflix-ui
  labels:
    app: netflix-ui
spec:
  type: NodePort
  selector:
    app: netflix-ui
  ports:
  - port: 80
    targetPort: 80
    nodePort: 31000
